# Regenerated by the scripts — never commit build output
.work/
eval/results.json
__pycache__/
*.pyc

# Secrets — never commit. app/run.sh sources .env if it exists.
.env
.env.*
!.env.example
*.key

# internal working notes, not for the public repo
UI_PORT_PROMPT.md
